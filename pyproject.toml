[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"

[tool.poetry]
name = "mail-tuner"
version = "1.0.0"
description = "Mail Tuner"

[tool.poetry.dependencies]
python = "^3.8"
